package devices.zeroshot.smollm;

/*
Q: Provide an implementation for this java interface of a Device: "public interface Device {
    void on() throws IllegalStateException;
    void off();
    boolean isOn();
    void reset();
}
"
*/

/*
// A:

interface Device {
    int on(int state); // on() method
    void off(void state); // off() method
    
    void reset(); // reset() method
};
*/

// Conclusion: the implementation of the Device interface is incorrect, it is not an implementation but a new interface definition.