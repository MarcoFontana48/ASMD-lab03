package devices.fewshot.smollm;

//smollm generated once again an interface that is not an implementation of the Device interface
interface LightSwitch {
    boolean on();
    void off();
    boolean isOn();
    void reset();
};